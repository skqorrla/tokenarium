# 런타임 의존성
click>=8.0         # CLI 명령어
textual>=0.50      # 터미널 TUI
watchdog>=6.0      # 파일 감시 (미설치 시 polling fallback 자동 동작)
openpyxl>=3.1      # docs/*.xlsx 시드 적재 (python -m aqua seed)

# 개발 의존성
pytest>=9.0        # 테스트 러너
